# Atlas Environment Configuration
# Copy this to .env and fill in real values
# NEVER COMMIT .env - only .env.example

# ============================================================
# Database (Required)
# ============================================================
# PRIMARY CONNECTION - Use the Session Pooler (recommended)
# This is IPv4-compatible and works from any network.
# Get from: Supabase Dashboard > Settings > Database > Connection String > Session Pooler
#
# Format: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres
DATABASE_URL='postgresql://postgres.[project-ref]:[password]@aws-0-us-west-2.pooler.supabase.com:5432/postgres?sslmode=require'

# ------------------------------------------------------------
# Alternative Connection URLs (Optional)
# ------------------------------------------------------------
# Session Pooler (explicit) - same as DATABASE_URL above
# Use port 5432 for session mode (connection-per-session, full feature support)
# DATABASE_POOLER_URL='postgresql://postgres.[project-ref]:[password]@aws-0-us-west-2.pooler.supabase.com:5432/postgres?sslmode=require'

# Transaction Pooler - use port 6543 for high-concurrency workloads
# Note: Some features (LISTEN/NOTIFY, prepared statements) don't work in transaction mode
# DATABASE_TRANSACTION_URL='postgresql://postgres.[project-ref]:[password]@aws-0-us-west-2.pooler.supabase.com:6543/postgres?sslmode=require'

# Direct Connection - connects directly to db.[project-ref].supabase.co
# WARNING: Often IPv6-only, will fail on networks without IPv6 support
# Only use if you specifically need direct access (migrations, extensions, etc.)
# DATABASE_DIRECT_URL='postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres?sslmode=require'

# ============================================================
# Feature Flags (Optional)
# ============================================================
# Show TEST MODE banner in UI
NEXT_PUBLIC_TEST_MODE=1

# Enable write operations (default: disabled for safety)
# ALLOW_WRITES=1

# Enable soft-fail mode for demos when DB is unreachable
# DB_SOFT_FAIL=1

# ============================================================
# External APIs (Optional)
# ============================================================
# Google Places API (for address autocomplete)
# GOOGLE_PLACES_API_KEY=AIza...

# Airtable Personal Access Token (for sync - future)
# AIRTABLE_PAT=pat...

# ============================================================
# Local Data Paths (Reference Only)
# ============================================================
# Where Ben's local exports live (never committed)
# LOCAL_INGEST_PATH=/Users/benmisdiaz/Desktop/AI_Ingest
