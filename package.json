{
  "name": "atlas",
  "version": "1.0.0",
  "private": true,
  "description": "Trapper Operations Data Platform - A stable, trustworthy system for managing trapping locations, requests, and clinic schedules.",
  "scripts": {
    "dev": "npm --prefix apps/web run dev",
    "build": "npm --prefix apps/web run build",
    "start": "npm --prefix apps/web run start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.2",
    "dotenv": "^17.3.1",
    "openai": "^6.25.0",
    "pg": "^8.19.0",
    "xlsx": "^0.18.5",
    "xml2js": "^0.6.2"
  },
  "devDependencies": {
    "csv-parse": "^6.1.0"
  }
}
